{
  "$schema": "https://raw.githubusercontent.com/Metta-AI/metta/main/packages/coworld/src/coworld/coworld_manifest_schema.json",
  "tags": [
    "halite",
    "kaggle",
    "economy",
    "free-for-all"
  ],
  "episode_timeout_minutes": 20,
  "game": {
    "name": "halite",
    "replay_viewer": {
      "bundle": "static-replay-viewer"
    },
    "description": "A bit-exact port of Kaggle's Halite IV (kaggle-environments 1.32.7). Four fleets mine a 21x21 wrap-around board of halite, haul it home to their shipyards and ram each other: when two ships end a turn on the same cell the one carrying LESS survives and takes the other's cargo, and equal cargo kills both. Halite in a hold is worth nothing and is stealable; halite in the bank is worth everything and can never be lost. Mining longer earns more but makes you heavier, and heavy loses every collision. Most banked halite at turn 400 wins. Turn resolution is the vendored upstream code itself, not a re-implementation, and a differential fidelity gate proves it every CI run. Policies issue a 20-turn fleet directive; a deterministic micro layer moves every ship.",
    "owner": "daveey@softmax.com",
    "runnable": {
      "type": "game",
      "image": "{{HALITE_IMAGE}}",
      "run": [
        "python",
        "-m",
        "cogame_halite.server"
      ],
      "source_url": "https://github.com/Metta-AI/cogame-halite/tree/main",
      "resources": {
        "requests": {
          "cpu": "1",
          "memory": "1Gi"
        }
      }
    },
    "config_schema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "tokens",
        "players",
        "num_agents"
      ],
      "properties": {
        "tokens": {
          "description": "One connection token per seat, indexed by seat slot. Injected by the runner; never authored.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "players": {
          "description": "One player display-name object per seat, indexed by seat slot.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "name"
            ],
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1
              }
            }
          }
        },
        "num_agents": {
          "description": "Seat count. cogame-halite is a four-seat free-for-all in every variant; the platform ladder reads this to schedule episodes.",
          "type": "integer",
          "minimum": 4,
          "maximum": 4,
          "default": 4
        },
        "seed": {
          "description": "Board-generation seed. Omitted in every variant so each episode draws a fresh one; always recorded in results and in the replay header.",
          "type": "integer",
          "minimum": 0,
          "maximum": 2147483647
        },
        "episode_steps": {
          "description": "Turns in the episode, upstream's episodeSteps. Turn 0 is the generated board; Board.next() is applied episode_steps - 1 times.",
          "type": "integer",
          "minimum": 2,
          "maximum": 2000,
          "default": 400
        },
        "starting_halite": {
          "description": "Board halite at turn 0, upstream's startingHalite (the normalisation target for populate_board).",
          "type": "integer",
          "minimum": 1000,
          "maximum": 200000,
          "default": 24000
        },
        "directive_every": {
          "description": "A seat is asked for a fresh 20-turn directive on turns where turn mod directive_every == 0; every other turn is answered from the compiled plan.",
          "type": "integer",
          "minimum": 1,
          "maximum": 400,
          "default": 20
        },
        "turn_deadline_ms": {
          "description": "Shared deadline for the parallel batch on a micro turn.",
          "type": "integer",
          "minimum": 10,
          "maximum": 60000,
          "default": 400
        },
        "directive_deadline_ms": {
          "description": "Shared deadline for the parallel batch on a directive turn (covers the player's 12 s attempt plus its 5 s retry plus transport).",
          "type": "integer",
          "minimum": 100,
          "maximum": 120000,
          "default": 18000
        },
        "directive_spacing_ms": {
          "description": "The engine will not open a new directive batch until this long after the previous one opened. Four calls per batch at >= 10 s is 24 req/min, under the Bedrock sidecar's 30 req/min per-episode cap.",
          "type": "integer",
          "minimum": 0,
          "maximum": 120000,
          "default": 10000
        },
        "player_connect_timeout_seconds": {
          "description": "Lobby bound. A seat that never connects plays the server-side tidewalker fallback; it never stops the clock.",
          "type": "number",
          "minimum": 1,
          "maximum": 900,
          "default": 120
        },
        "wall_clock_budget_seconds": {
          "description": "Hard stop at a turn boundary -> results.reason = \"deadline\", settled by the same scoring ladder. 55% of the assumed 1200 s episode timeout.",
          "type": "number",
          "minimum": 5,
          "maximum": 3600,
          "default": 660
        },
        "budget_guard_seconds": {
          "description": "Past this the engine stops asking players anything and every seat plays the in-process tidewalker compile, so the episode still ends complete. Must be below wall_clock_budget_seconds.",
          "type": "number",
          "minimum": 1,
          "maximum": 3600,
          "default": 600
        },
        "size": {
          "description": "Board edge. PINNED to upstream's default 21.",
          "type": "integer",
          "minimum": 21,
          "maximum": 21,
          "default": 21
        },
        "spawn_cost": {
          "description": "PINNED to upstream's default 500.",
          "type": "integer",
          "minimum": 500,
          "maximum": 500,
          "default": 500
        },
        "convert_cost": {
          "description": "PINNED to upstream's default 500.",
          "type": "integer",
          "minimum": 500,
          "maximum": 500,
          "default": 500
        },
        "move_cost": {
          "description": "PINNED to upstream's default 0.0.",
          "type": "number",
          "minimum": 0,
          "maximum": 0,
          "default": 0.0
        },
        "collect_rate": {
          "description": "PINNED to upstream's default 0.25.",
          "type": "number",
          "minimum": 0.25,
          "maximum": 0.25,
          "default": 0.25
        },
        "regen_rate": {
          "description": "PINNED to upstream's default 0.02.",
          "type": "number",
          "minimum": 0.02,
          "maximum": 0.02,
          "default": 0.02
        },
        "max_cell_halite": {
          "description": "PINNED to upstream's default 500.",
          "type": "integer",
          "minimum": 500,
          "maximum": 500,
          "default": 500
        }
      }
    },
    "results_schema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "names",
        "aliases",
        "scores",
        "placement",
        "ranking",
        "win",
        "winner",
        "reason",
        "end_rule",
        "final_turn",
        "seed",
        "banked",
        "ships",
        "yards",
        "mined",
        "stolen",
        "collisions_won",
        "collisions_lost",
        "eliminated_turn",
        "llm_turns",
        "fallbacks",
        "dead_seats",
        "stop_detail"
      ],
      "properties": {
        "names": {
          "description": "Real policy names, seat order (spectator side only).",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "string"
          }
        },
        "aliases": {
          "description": "FLEET-ALPHA .. FLEET-DELTA, seat order.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "string"
          }
        },
        "scores": {
          "description": "Banked halite, or eliminated_turn - episode_steps - 1 when eliminated. Higher is better.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "integer"
          }
        },
        "placement": {
          "description": "1..4 with the tie-break ladder; equal seats share the higher number.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "integer"
          }
        },
        "ranking": {
          "description": "Strict seat order, ladder rule 4 (seat index) applied.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "integer"
          }
        },
        "win": {
          "description": "placement == 1.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "boolean"
          }
        },
        "winner": {
          "description": "The single seat with placement == 1, or null when two or more share it.",
          "type": [
            "integer",
            "null"
          ]
        },
        "reason": {
          "description": "Closed enum.",
          "type": "string",
          "enum": [
            "complete",
            "deadline",
            "fault"
          ]
        },
        "end_rule": {
          "description": "Which end condition fired.",
          "type": "string",
          "enum": [
            "full_time",
            "last_fleet",
            "wall_clock",
            "fault"
          ]
        },
        "final_turn": {
          "description": "The last recorded turn.",
          "type": "integer"
        },
        "seed": {
          "description": "The episode seed (recorded so the replay re-derives).",
          "type": "integer"
        },
        "banked": {
          "description": "Final bank per seat.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "integer"
          }
        },
        "ships": {
          "description": "Ships alive at the end.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "integer"
          }
        },
        "yards": {
          "description": "Shipyards alive at the end.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "integer"
          }
        },
        "mined": {
          "description": "Lifetime halite scraped.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "integer"
          }
        },
        "stolen": {
          "description": "Lifetime halite taken in collisions.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "integer"
          }
        },
        "collisions_won": {
          "description": "Collisions this seat's ship survived.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "integer"
          }
        },
        "collisions_lost": {
          "description": "Collisions this seat lost a ship in.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "integer"
          }
        },
        "eliminated_turn": {
          "description": "The turn the seat was eliminated, or null.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "llm_turns": {
          "description": "Turns answered with source llm|retry.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "integer"
          }
        },
        "fallbacks": {
          "description": "Server-side substitutions, partitioned by cause.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "timeout",
              "malformed",
              "wrong_turn",
              "disconnected",
              "host_error"
            ],
            "properties": {
              "timeout": {
                "type": "integer"
              },
              "malformed": {
                "type": "integer"
              },
              "wrong_turn": {
                "type": "integer"
              },
              "disconnected": {
                "type": "integer"
              },
              "host_error": {
                "type": "integer"
              }
            }
          }
        },
        "dead_seats": {
          "description": "Strike-rule flag at the end.",
          "type": "array",
          "minItems": 4,
          "maxItems": 4,
          "items": {
            "type": "boolean"
          }
        },
        "stop_detail": {
          "description": "Empty, or the fault text (<= 200 runes).",
          "type": "string"
        }
      }
    },
    "protocols": {
      "player": {
        "type": "uri",
        "value": "https://github.com/Metta-AI/cogame-halite/blob/main/docs/PROTOCOL.md"
      },
      "global": {
        "type": "uri",
        "value": "https://github.com/Metta-AI/cogame-halite/blob/main/docs/PROTOCOL.md"
      }
    },
    "docs": {
      "readme": {
        "type": "text",
        "value": "# cogame-halite\n\nA **four-seat, free-for-all economy race with physical risk on every move**: a\nbit-exact Coworld port of **Kaggle's Halite IV** (`kaggle-environments`, Two\nSigma seasons I–IV).\n\nFour fleets mine a 21 × 21 torus of halite, haul it home to their shipyards,\nand ram each other — the ship carrying *less* survives the collision and takes\nthe other's cargo. Most banked halite at turn 400 wins.\n\n* Rules and bit-exactness: [`docs/RULES.md`](docs/RULES.md)\n* Wire protocol: [`docs/PROTOCOL.md`](docs/PROTOCOL.md)\n* Replay format + viewer contract: [`docs/REPLAY.md`](docs/REPLAY.md)\n* Design note: [`docs/plans/2026-08-27-halite-design.md`](docs/plans/2026-08-27-halite-design.md)\n* Working conventions: [`AGENTS.md`](AGENTS.md)\n"
      },
      "pages": [
        {
          "id": "rules.md",
          "title": "Rules & bit-exactness",
          "content": {
            "type": "text",
            "value": "# Rules & bit-exactness\n\n**Four fleets, one salt flat, four hundred turns.** cogame-halite is a\nbit-exact port of **Kaggle's Halite IV** (`kaggle-environments`, Two Sigma\nseasons I–IV; the shipped env is season IV) onto the Coworld platform.\n\n## The game in one paragraph\n\nEvery fleet starts with one ship and 5 000 banked halite in its own quadrant of\na 21 × 21 wrap-around board holding about 24 000 halite in uneven clusters. A\nship that holds still scrapes **25 %** of the halite under it into its hold. A\nship that moves carries its hold with it — and a hold is a target: when two\nships end a turn on the same cell, the one carrying **less** survives and\n**takes the other's cargo**, and if they carry exactly the same, both are\ndestroyed. Cargo only becomes score when a ship ends its move on one of its own\nshipyards. You get shipyards by burning a ship and 500 halite (`CONVERT`); you\nget ships by burning 500 banked halite at a shipyard (`SPAWN`). An enemy ship\nthat walks onto your shipyard destroys the shipyard and itself. Everyone moves\nat once, every turn, with no information hidden.\n\nThe tension: halite in a hold is worth nothing and is *stealable*; halite in\nthe bank is worth everything and can never be lost. Mining longer earns more\nbut makes you heavier, and heavy loses every collision.\n\n## Rule constants\n\nAll upstream defaults, all served unchanged, all **read from the vendored\n`halite.json`** rather than re-typed (`server/cogame_halite/defaults.py`):\n\n| Constant | Value | Upstream key |\n|---|---|---|\n| Board edge | 21 (441 cells, wraps in both axes) | `size` |\n| Turns in an episode | 400 | `episodeSteps` |\n| Board halite at turn 0 | 24 000 | `startingHalite` |\n| Bank per fleet at turn 0 | 5 000 | `reward.default` |\n| Mine rate | 0.25 of the cell, `int()`-truncated | `collectRate` |\n| Regeneration rate | 0.02 per turn, on cells with no ship | `regenRate` |\n| Cell cap | 500 | `maxCellHalite` |\n| Ship cost | 500 | `spawnCost` |\n| Convert cost | 500 | `convertCost` |\n| Move cost | 0.0 | `moveCost` |\n| Actions | `NORTH`, `SOUTH`, `EAST`, `WEST`, `CONVERT` (ships), `SPAWN` (shipyards), plus *no action* = mine | `action.enum` |\n\n`size`, `spawnCost`, `convertCost`, `moveCost`, `collectRate`, `regenRate` and\n`maxCellHalite` are **pinned** in `config_schema` (min == max == the upstream\ndefault), so a variant cannot drift them. Only `episode_steps` and\n`starting_halite` vary between variants, and both are first-class upstream\nconfig fields — the rules stay bit-exact in every variant.\n\n## Geometry\n\n* A cell is `Point(x, y)` with `x` rightwards and **`y` upwards**;\n  `index = (size - y - 1) * size + x`. **Index 0 is the top-left cell on\n  screen** and index 440 the bottom-right, so the viewer draws the `halite`\n  array in raster order and gets upstream's own orientation for free.\n* `NORTH = (0, +1)` = `index - size` = **up on screen**; `SOUTH` down; `EAST`\n  `+1`; `WEST` `-1`; all four wrap.\n* Distances are torus Manhattan.\n* Asset ids are strings minted `f\"{turn}-{n}\"`, `n` counting from 1 across\n  **all** players within one resolution. The four opening ships are `0-1`,\n  `0-2`, `0-3`, `0-4`.\n* `populate_board` mirrors one 11 × 11 quartile into all four quadrants, so the\n  board is exactly 4-fold symmetric and the four starting cells are equivalent\n  by construction: seat 0 at index **110** = (5, 15), seat 1 at **120** =\n  (15, 15), seat 2 at **320** = (5, 5), seat 3 at **330** = (15, 5). No seat\n  can be dealt a better island, so no seat randomisation is applied.\n\n## Turn resolution — the exact order\n\nOne call of `sim.step(orders)` is the vendored `Board.next()` followed by the\nelimination block of `interpreter()`. **This is not a re-implementation: the\nvendored `Board` class is imported and called.** The order is written out so a\nreader knows what the game *is*, and so `tests/test_sim.py` has a numbered list\nto assert against.\n\n1. **Bind actions.** Each ship and shipyard takes at most one action. A value\n   not in the enum, or an id the seat does not own, is simply **not bound**.\n   `SPAWN` on a ship and a direction on a shipyard are likewise not bound.\n2. **Per-seat action processing, seats in ascending seat order.**\n   1. **`SPAWN`**, shipyards in insertion order. If `bank >= 500`:\n      `bank -= 500` and a new ship with 0 cargo appears on that cell. A seat\n      that orders more spawns than it can pay for gets the ones its bank\n      covers, in that order.\n   2. **`CONVERT`**, ships in insertion order. Allowed only if the cell holds\n      **no shipyard** (of anyone) and `ship.cargo + bank >= 500`. Then\n      `delta = cargo - 500`; `bank += min(delta, 0)` immediately;\n      `max(delta, 0)` is **held aside** and added only after all of this seat's\n      converts (upstream's guard against chaining one convert into another); a\n      shipyard appears, **the cell's halite is set to 0**, the ship is deleted.\n   3. **Moves.** One cell with wrap. `moveCost` is 0, so cargo is unchanged.\n      Two ships may land on the same cell — that is the point.\n   4. `bank += leftover_convert_halite`, then `assert bank >= 0`.\n3. **Ship-to-ship collisions**, over every cell holding more than one ship. The\n   **strictly smallest cargo survives** and **absorbs the cargo of every ship\n   destroyed there**. A tie for smallest destroys them all. Ownership is\n   irrelevant: friendly ships collide on exactly the same rule.\n4. **Ship-to-shipyard collisions.** A ship of a **different** seat standing on\n   a shipyard destroys both.\n5. **Deposit.** A surviving shipyard with its **own** seat's ship on it banks\n   that ship's cargo and zeroes it. This is the only way halite is ever banked.\n6. **Mining.** `delta = int(cell.halite * 0.25)`. A ship mines iff its bound\n   action was **not** a move, the cell holds **no shipyard**, and `delta > 0`.\n   Cargo is uncapped.\n7. **Regeneration.** For every cell **with no ship on it**:\n   `halite = min(round(halite * 1.02, 3), 500)`. The `round(x, 3)` is Python's\n   round-half-to-even and is part of the contract.\n8. **Turn counter.** `turn += 1`.\n9. **Elimination.** A seat still active whose ships are all gone **and** which\n   either has no shipyard or has `bank < 500` is eliminated at this turn: it\n   can never act again and its score freezes at `turn - episode_steps - 1`.\n10. **Last-fleet check.** Fewer than two active seats ends the episode.\n\nThings a reader will otherwise get wrong, each an upstream fact:\n\n* Spawn is processed **before** convert within a seat, and a seat's whole block\n  before the next seat's — but no seat sees another's actions, so the ordering\n  only decides how a *single* seat's bank is spent.\n* Collisions resolve **after all movement**, so head-on swaps (A→B, B→A) do\n  **not** collide in Halite IV. There is no swap rule.\n* Deposit happens **after** collisions: a loaded ship rammed on the doorstep of\n  its own shipyard loses everything.\n* A cell that becomes a shipyard loses its halite permanently.\n* A ship standing still suppresses that cell's regeneration *and* mines it.\n\n## Scoring\n\n```\nscore[s] = banked[s]                        if eliminated[s] is null\n         = eliminated[s] - episode_steps - 1  otherwise   (negative)\n```\n\n**Higher is better.** A surviving fleet always outranks an eliminated one, and\namong eliminated fleets, surviving longer ranks higher — Kaggle's own\nleaderboard rule. `results.placement[s]` ∈ {1,2,3,4} uses this ladder, first\ndifference deciding:\n\n1. `score` descending\n2. `shipyards + ships` at the end, descending\n3. `mined` (lifetime halite scraped) descending\n4. seat index ascending (a total order, so it always terminates)\n\nSeats still equal after rule 3 share the higher `placement` (1,1,3,4); rule 4\nstill gives `results.ranking` a strict order. `results.win[s]` is\n`placement[s] == 1`; `results.winner` is the single seat with `placement == 1`,\nor `null` when two or more share it. A `deadline` episode is scored by the same\nformula at the turn the clock stopped — never zeroed, always rankable.\n\n## End conditions\n\n| `end_rule` | When | `results.reason` |\n|---|---|---|\n| `full_time` | The state at the last turn has been recorded. | `complete` |\n| `last_fleet` | Fewer than two seats remain active. | `complete` |\n| `wall_clock` | `wall_clock_budget_seconds` (660 s) reached at a turn boundary. | `deadline` |\n| `fault` | An unhandled exception in the sim or the loop. | `fault` |\n\n`results.reason` is a closed enum of exactly those three values. A seat that\nnever connects, disconnects, or fails every reply **does not end the episode**:\nits fleet plays the server-side `tidewalker` fallback and the game runs to its\nnatural end.\n\n## Why this is bit-exact, and how you can check\n\nThe port **imports** the vendored upstream modules and calls\n`Board(obs, config, actions).next()`. `vendor/upstream/` is byte-pristine with\na sha256 per file; `sim/assemble.py` copies it into `build/khalite/` and adds\nthree package `__init__.py` files and nothing else. Two upstream code paths are\nreached differently and are named in `vendor/PATCHES.md` with their citations:\nthe `populate_board` adapter and the 12-line elimination block.\n\n`tests/test_fidelity.py` is the acceptance criterion and a permanent CI gate.\nWith `kaggle-environments==1.32.7` installed from the CI-only `fidelity`\ndependency group it drives upstream's own `make(\"halite\")` env and our sim over\nthe same order stream for **8 seeds × 399 turns** and asserts equality of the\nfull observation at every turn — the 441-entry `halite` list element for\nelement (exact floats), every player's `[bank, shipyards, ships]` including\n**dict insertion order**, `step`, and each agent's `status`/`reward` — plus\n50-seed board generation and the four starting positions. Any divergence fails.\n\n## Not in v1\n\nKaggle's HTML replay renderer (this repo ships its own static wasm bundle);\nHalite seasons I–III; 1- and 2-player agent counts; `remainingOverageTime` as a\nreal time bank; the open Kaggle bot corpus as policies; automated\nalliance-pattern audit tooling; a live spectator pod of any kind.\n"
          }
        },
        {
          "id": "replay.md",
          "title": "Replay format",
          "content": {
            "type": "text",
            "value": "# Replay format & viewer contract\n\nA replay is a single **UTF-8 JSON** document (extension `.replay`), written\nonce at the end of the episode and also streamable to disk turn by turn. It is\n**self-sufficient by construction**: everything the viewer needs is in the\nbytes, and the viewer contacts S3 and nothing else.\n\n## The document\n\n```json\n{\"format\":\"cogame-halite-replay\",\"version\":1,\"gameVersion\":\"1.0.0\",\"protocol\":\"halite/1\",\n \"coworld\":\"halite\",\"seed\":8675309,\n \"config\":{ …every resolved game-config field, tokens excluded… },\n \"names\":[\"daveey\",\"daveey-1\",\"halite-tidewalker\",\"halite-corsair\"],\n \"aliases\":[\"FLEET-ALPHA\",\"FLEET-BRAVO\",\"FLEET-CHARLIE\",\"FLEET-DELTA\"],\n \"policySources\":[\"llm\",\"llm\",\"scripted:tidewalker\",\"scripted:corsair\"],\n \"colors\":[\"#e8a33d\",\"#3fb6b0\",\"#c65fa8\",\"#8fbf3f\"],\n \"turns\":[{\"t\":0,\n           \"halite\":[<441 integers, round(cell)>],\n           \"players\":[[<bank>,{\"<yardId>\":<pos>},{\"<shipId>\":[<pos>,<cargo>]}], …4],\n           \"orders\":[{\"<assetId>\":\"<ACTION>\"}, …4],\n           \"events\":[…],\n           \"hash\":\"9f2a41c07be31d55\"}, …],\n \"results\":{ …the full results document… },\n \"stop\":{\"rule\":\"full_time\",\"turn\":399}}\n```\n\n* The per-turn `halite` array is **rounded to integers** because that is what is\n  drawn. The exact float state is pinned by `hash`, and by `orders` + `seed`,\n  which let CI re-derive the episode exactly.\n* `hash` is **FNV-1a 64** over a canonical encoding — `turn`, then each cell's\n  halite formatted `%.3f` in index order, then per seat the `bank`, the sorted\n  `(yard_id, pos)` pairs and the sorted `(ship_id, pos, cargo)` triples, then\n  the elimination turns. It sorts assets, so it pins *state*, not serialisation\n  order, and it is stable across processes (no `PYTHONHASHSEED` dependency).\n* `stop` is **one load-bearing record applied by the same constructor on record\n  and on re-derive**. A wall-clock stop is a wall-clock fact that cannot be\n  recomputed from sim state, so it is recorded, not inferred.\n  `tests/test_replay.py` runs the record → re-derive check for **every** end\n  reason, not just `complete`.\n* Size: roughly 3 kB/turn, so ≈ 1.3 MB for a 400-turn episode and ≈ 400 kB for\n  the 120-turn CI fixture.\n\n## Events\n\nOne `events` array per turn, each `{\"k\": <kind>, …}`, in the order the\nresolution produced them. This is the complete vocabulary\n(`server/cogame_halite/events.py`); `tests/test_replay.py` schema-checks every\nkind and `tests/test_viewer.py` asserts a `.beat-marker.<kind>` CSS rule for\nevery kind that places a scrubber beat.\n\n| kind | payload | drawn as |\n|---|---|---|\n| `spawn` | `seat, ship, pos` | a pop at the yard, +1 on the seat's ship count |\n| `convert` | `seat, ship, yard, pos` | the new dock stamps in, scrubber **beat** (`yard`) |\n| `deposit` | `seat, ship, yard, pos, amount` | coin arc into the plate, bank ticks up |\n| `mine` | `seat, ship, pos, amount` | the cell dims one step, the hull pip grows |\n| `collide` | `pos, survivor:{seat,ship}\\|null, lost:[{seat,ship,cargo}], stolen` | flash + shards, feed line, scrubber **beat** (`ram`) |\n| `yardraze` | `pos, yardSeat, yard, shipSeat, ship` | the dock cracks, scrubber **beat** (`raze`) |\n| `eliminate` | `seat, turn` | the plate greys out, scrubber **beat** (`out`) |\n| `lead` | `seat, bank` | the crown moves, scrubber **beat** (`lead`); emitted only on a change |\n| `note` | `seat, text (≤140 runes), source, latencyMs` | a speech line in the feed |\n| `fallback` | `seat, cause, detail (≤120 runes)` | a small grey chip in the feed |\n| `strike` | `seat` | the plate is marked \"silent\" |\n| `budget_guard` | `turn` | a feed line, scrubber **beat** (`guard`) |\n| `stop` | `rule, turn` | the endcard's win-condition chip |\n\n## Strict UTF-8\n\nEvery string that can reach the replay is truncated on **rune** boundaries\n(`server/cogame_halite/defaults.py::truncate_runes`). A byte-boundary\ntruncation splits a multi-byte character and produces bytes that render in a\nbrowser and fail a strict parser; `tests/test_replay.py` feeds emoji-laden\nnotes through the whole path and parses the result with `bytes.decode(\"utf-8\")`\n(strict) plus `json.loads`.\n\n## The viewer bundle\n\n`game.replay_viewer.bundle = \"static-replay-viewer\"`, built by\n`tools/build_replay_viewer.sh` (the `coworld build` hook) from the Dockerfile's\n`wasm-builder` stage. The bundle is:\n\n```\nindex.html                      client/replay_broadcast.html\nchrome_common.js                coworld-ctf, BYTE-FOR-BYTE\nbroadcast_core.js               coworld-ctf, BYTE-FOR-BYTE (loaded by the Worker only)\nstatic_replay.js                coworld-ctf + the four documented adaptations\nstatic_replay_worker.js         coworld-ctf + the four documented adaptations\nhalite_replay.{js,wasm,data}    replay-viewer/halite_replay.nim -> emscripten\nfont.ttf                        the Rajdhani face the chrome's @font-face loads\n```\n\nThe adaptations — the three `cogame-factorio` already made to the same four\nfiles, plus one this game forces:\n\n1. `start()` takes the replay bytes the page fetched (one fetch serves both the\n   chrome and the renderer),\n2. the sim mismatch-tick attribute is gone — **nothing is re-simulated in the\n   browser**; the wasm renderer draws the *recorded* per-turn state,\n3. the exported symbols are renamed `_halite_*`,\n4. the Worker's `importScripts` drops ctf's `wire_constants.js`. That file is\n   not in the ctf tree: ctf **generates** it during its own image build\n   (`tools/gen_wire_constants.nim` → `replay-viewer/dist/wire_constants.js`)\n   and it carries ctf's paintball wire enums, which nothing here reads.\n   Importing a file the bundle does not ship would 404 the Worker's boot on\n   every load. It is named in `static_replay.js`'s header, at the\n   `importScripts` call site, and in the design note's deviations appendix;\n   `tests/test_viewer.py` pins the call.\n\nThe page owns playback (which turn) and tells the renderer on the sprite\nprotocol's text channel: `s:<turn>` and `r:<0|1>` (the cargo-at-risk overlay).\nChrome JSON rides the reserved sprite 4090's label.\n\n`data-replay-loaded=\"true\"` lands on `<html>` on the first drawn frame and\n`data-replay-error=\"<message>\"` on failure; the `coworld-replay` bridge `ready`\nmessage is posted from a `MutationObserver` callback that fires **after**\n`data-replay-loaded` is set, never on rAF timing at the call site.\n\n### What it draws\n\n* **Board.** 21 × 21 cells on the tiling seabed. Each cell's halite is a\n  crystal sprite at one of six density steps (0, 1–49, 50–149, 150–299,\n  300–449, 450–500). Ships are the seat-coloured hull with a **cargo pip**\n  whose size and glow scale with cargo; shipyards are the seat-coloured dock.\n* **Cargo-at-risk overlay.** A ship is **at risk** iff some enemy ship with\n  cargo **≤** its own is within torus Manhattan distance 1 — the exact\n  predicate the ram rule uses next turn. At-risk ships get a pulsing red halo\n  and the ≤ 4 cells a lighter enemy could ram them from get a faint red wash.\n  Always on; `r` toggles it.\n* **Scorebug.** Four plates, two per column: colour swatch, alias, **real\n  policy name**, banked halite (big, tabular), cargo afloat, ships, yards,\n  halite at risk, a crown on the leader, a grey wash on an eliminated seat.\n* **Clock.** `TURN 137 / 399` plus the caption `mining` / `hauling` /\n  `raiding`, derived from the turn's event mix.\n* **Feed.** The last ~12 events in words, plus each LLM `note` as a speech line\n  under its alias.\n* **Endcard.** Final standings 1–4 with alias + real name, banked, mined,\n  stolen, rams won/lost, ships built, elimination turn and the win-condition\n  chip from `stop.rule`.\n* **Playback.** 125 ms per turn at 1× (a 400-turn episode plays in 50 s; the\n  120-turn CI fixture in 15 s, which outlasts the 12 s viewer soak), speeds\n  from the inherited chrome.\n* **Legible at 360 px wide.** A stated acceptance property, checked by\n  `viewer_smoke.mjs` through `tools/ci/narrow_fixture.html` as well as at\n  desktop size, and by `tools/ci/renderer_fixture.html` at three canvas sizes\n  with a full-cap 140-rune note on every seat.\n"
          }
        }
      ]
    }
  },
  "player": [
    {
      "id": "tidewalker",
      "type": "player",
      "name": "tidewalker",
      "description": "The published scripted baseline and the executor of every LLM directive: mine the richest cell nearby, come home when full, never end a turn beside a lighter enemy.",
      "image": "{{HALITE_IMAGE}}",
      "run": [
        "python",
        "-m",
        "players.halite_player"
      ],
      "env": {
        "PLAYER_SCRIPTED": "tidewalker"
      },
      "source_url": "https://github.com/Metta-AI/cogame-halite/tree/main/players",
      "resources": {
        "requests": {
          "cpu": "250m",
          "memory": "256Mi"
        },
        "limits": {
          "cpu": "1"
        }
      }
    },
    {
      "id": "corsair",
      "type": "player",
      "name": "corsair",
      "description": "The raider baseline: light hulls, a lower return threshold and hunting always on, so the cargo-theft collision rule is exercised in every episode including the all-scripted CI smoke.",
      "image": "{{HALITE_IMAGE}}",
      "run": [
        "python",
        "-m",
        "players.halite_player"
      ],
      "env": {
        "PLAYER_SCRIPTED": "corsair"
      },
      "source_url": "https://github.com/Metta-AI/cogame-halite/tree/main/players",
      "resources": {
        "requests": {
          "cpu": "250m",
          "memory": "256Mi"
        },
        "limits": {
          "cpu": "1"
        }
      }
    }
  ],
  "variants": [
    {
      "id": "standard",
      "name": "Standard (400 turns)",
      "description": "The full Halite IV episode: 21x21 torus, 24000 halite, four fleets, 400 turns, a fresh LLM directive every 20 turns.",
      "game_config": {
        "players": [
          {
            "name": "Fleet A"
          },
          {
            "name": "Fleet B"
          },
          {
            "name": "Fleet C"
          },
          {
            "name": "Fleet D"
          }
        ],
        "num_agents": 4,
        "episode_steps": 400,
        "starting_halite": 24000,
        "directive_every": 20,
        "turn_deadline_ms": 400,
        "directive_deadline_ms": 18000,
        "directive_spacing_ms": 10000,
        "player_connect_timeout_seconds": 120,
        "wall_clock_budget_seconds": 660,
        "budget_guard_seconds": 600
      }
    },
    {
      "id": "sprint",
      "name": "Sprint (200 turns)",
      "description": "Same rules, half the episode, a directive every 10 turns. Exists for ladder throughput, not because the budget needs it.",
      "game_config": {
        "players": [
          {
            "name": "Fleet A"
          },
          {
            "name": "Fleet B"
          },
          {
            "name": "Fleet C"
          },
          {
            "name": "Fleet D"
          }
        ],
        "num_agents": 4,
        "episode_steps": 200,
        "starting_halite": 24000,
        "directive_every": 10,
        "turn_deadline_ms": 400,
        "directive_deadline_ms": 18000,
        "directive_spacing_ms": 10000,
        "player_connect_timeout_seconds": 120,
        "wall_clock_budget_seconds": 660,
        "budget_guard_seconds": 600
      }
    },
    {
      "id": "richfields",
      "name": "Rich fields",
      "description": "A denser board (32000 halite at turn 0), so more cells are worth contesting and the collision rule bites harder. Rules identical.",
      "game_config": {
        "players": [
          {
            "name": "Fleet A"
          },
          {
            "name": "Fleet B"
          },
          {
            "name": "Fleet C"
          },
          {
            "name": "Fleet D"
          }
        ],
        "num_agents": 4,
        "episode_steps": 400,
        "starting_halite": 32000,
        "directive_every": 20,
        "turn_deadline_ms": 400,
        "directive_deadline_ms": 18000,
        "directive_spacing_ms": 10000,
        "player_connect_timeout_seconds": 120,
        "wall_clock_budget_seconds": 660,
        "budget_guard_seconds": 600
      }
    }
  ],
  "certification": {
    "players": [
      {
        "player_id": "tidewalker"
      },
      {
        "player_id": "corsair"
      },
      {
        "player_id": "tidewalker"
      },
      {
        "player_id": "corsair"
      }
    ],
    "game_config": {
      "players": [
        {
          "name": "Tidewalker A"
        },
        {
          "name": "Corsair A"
        },
        {
          "name": "Tidewalker B"
        },
        {
          "name": "Corsair B"
        }
      ],
      "num_agents": 4,
      "episode_steps": 120,
      "seed": 42,
      "starting_halite": 24000,
      "directive_every": 20,
      "turn_deadline_ms": 400,
      "directive_deadline_ms": 18000,
      "directive_spacing_ms": 0,
      "player_connect_timeout_seconds": 60,
      "wall_clock_budget_seconds": 300,
      "budget_guard_seconds": 240
    }
  }
}
